/**
 * Application Constants
 * Central configuration for app-wide constants
 *
 * Note: The actual constant value is defined in constants.js so it can be used
 * by both JavaScript and TypeScript files throughout the project.
 * To change the domain, update constants.js
 */

import { SITE_URL as SITE_URL_JS } from './constants.js';

export const SITE_URL = SITE_URL_JS;
